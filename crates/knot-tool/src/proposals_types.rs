//! `knot-proposals` call types — included from the contract crate so the
//! wire format stays byte-identical (same pattern as `registry_types`).

#[allow(unused_imports)]
#[path = "../../knot-proposals/src/call_types.rs"]
pub mod call_types;
