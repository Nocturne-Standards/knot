# Knot static page (legacy copy)

Canonical marketing + demo page now lives in
`nocturne-standards-site/public/knot/` and deploys to
https://knot.nocturne-standards.org

Prefer editing and building from that tree (`npm run build:knot`).
This directory is kept as a non-canonical snapshot until removed.
