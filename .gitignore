/target/
**/target/
**/*.sqlite
.DS_Store
*.swp
*~

# Local symlink to aichbindas/nocturne-deployments pin home (see that repo README).
/deployments

# Pituitary local index (commit curated .toml)
.pituitary/*.db
.pituitary/*.db-*

# Git worktrees (kit-sync / tracks)
.worktrees/
