{
  "mcpServers": {
    "pituitary": {
      "command": "pituitary",
      "args": [
        "--config",
        "${workspaceFolder}/.pituitary/pituitary.toml",
        "serve",
        "--transport",
        "stdio"
      ]
    },
    "nocturne-mcp-gates": {
      "command": "node",
      "args": [
        "${workspaceFolder}/../nocturne-mcp-gates/mcp/server.mjs"
      ],
      "env": {
        "GATES_ROOT": "${workspaceFolder}",
        "NOCTURNE_AGENT_KIT": "${workspaceFolder}/../nocturne-agent-kit"
      }
    },
    "nocturne-mcp-tracks": {
      "command": "node",
      "args": [
        "${workspaceFolder}/../nocturne-mcp-tracks/mcp/server.mjs"
      ],
      "env": {
        "TRACKS_ROOT": "${workspaceFolder}"
      }
    }
  }
}
